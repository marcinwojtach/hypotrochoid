A simple 2d hypotrochoid figure.

## Requirements

* node.js > v.18

## Installation

Run `npm install`

## Run the program

`npm run dev` for development preview

or you can build it and serve with some other local server from `./dist` folder.

Run `npm run build`
